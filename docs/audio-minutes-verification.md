# 录音上传验收记录

验收日期：2026-09-17。分支：`codex/audio-minutes`，初始功能提交为 `a0096a2`，基线为原生产提交 `988c104`。主工作区原有未提交改动保持原样。首版仅开放录音上传，候选人与招聘方两种已登录角色均可使用；飞书妙记入口和接口均关闭。项目专用火山语音应用已完成真实转写验证并部署到生产。录音文件上限随后由 100 MiB 调整为 500 MiB，时长上限仍为 60 分钟。

## 任务清单

| Task | 主要工作 | 依赖与状态 |
|---|---|---|
| 自动化 | 功能、权限、持久化与失败恢复 | 175 项通过 |
| 浏览器 | 双模式确认、编辑失效、未配置回退与上传上限展示 | 既有 6 个场景通过；本次实机确认 500 MB 展示 |
| 构建 | Vite 生产产物 | 通过；保留已有大包提示 |
| 类型检查 | 与当前生产基线对比 | 两份输出完全相同，原有 PDF 模块错误尚存 |
| 真实联调 | 火山语音转写 | 18.45 秒中文合成访谈录音转写通过 |
| 生产发布 | 备份、迁移、平台依赖、代理规则与线上验收 | 已发布到 `https://evalbar.cn/` |

## 已完成验证

- `npm test`：175/175 通过，无跳过。新增及补充验证覆盖材料、客户端、录音处理、真实 HTTP 路由、500 MiB 前后端边界，以及首版飞书能力、路由默认关闭与生产装配显式关闭。
- `npm run build`：通过，当前 `dist/index.html` 引用 `/assets/index-TVWbONJU.js` 和 `/assets/index-Ba3jjskP.css`。HTML SHA-256：`c2318bbe03dd782e859c00e7de14f0641c669c5269702f1bbe4abcc154f94b04`。
- 四种合成音频经真实 `ffmpeg-static` 与 `music-metadata` 转换为 PCM WAV；损坏文件拒绝。这验证本地格式处理，不代表上游语音识别成功。
- 9 个材料 HTTP 测试覆盖未登录、跨用户、模式过滤、上传到确认、错误请求、体积上限、短时凭证撤销，以及默认不公开飞书能力或路由。使用真实 Express、内存 SQLite、临时文件与模拟供应商。
- 重启后不重复提交已知任务；查询失败只重查原编号；取消后迟到结果不会恢复任务；重试同样受全局 20 个活动任务限制。
- 报告与材料关联同事务提交；关联失败回滚报告。共享材料直到最后一份报告删除后才清理。
- 浏览器实际运行构建后的 React 页面，两种模式均验证只有录音入口，并覆盖上传、角色选择、人工修订、保存确认稿与分析请求 `materialId`/文字一致。确认后再次编辑会阻止下一步，须重新确认；未配置服务可回退文字输入。
- 本次限额调整通过本地合成登录和能力接口实机检查，页面显示“最大 500 MB、最长 60 分钟”；页面数值读取后端能力响应，不再单独硬编码。
- 桌面及手机视口检查通过，手机宽度 390 px 无横向溢出。截图仅含合成材料，输出到系统临时目录。
- 历史飞书模块保留为未挂载代码并继续由内部契约测试覆盖；生产路由工厂默认 `enableFeishu=false`，即使存在飞书配置也不会开放入口。
- 独立审查发现并已修复：角色选择未进入分析、集中重试绕过队列限制、提交检查文件时取消任务仍被重新排队。

## 真实 ASR 与生产验收

- 火山语音专用应用 `Eval-Bar-AI-ASR` 使用 App ID `6340636029`，只开通“豆包录音文件识别模型 2.0 标准版”。访问凭证只保存在生产 `.env.local`，未写入代码、提交或本文档。
- 生产运行参数已启用 `volc.seedasr.auc`，公开回调基址为 `https://evalbar.cn`。真实调用使用 18.45 秒中文合成访谈 WAV；第 2 次查询完成，返回 80 个文字字符、3 个时间片段，并识别出预期短语“招聘分析项目”。
- 发布前完整备份 `data/` 到 `/root/bar-raiser-ai-backups/data-before-audio-20260917-190737`。生产主机没有执行 Vite 构建；本地 `dist` 经 SHA-256 校验后分阶段上传并原子替换。
- 生产 PM2 进程 `bar-raiser-ai` 为 `online`。线上 HTML 加载 `index-TVWbONJU.js`，版本化资源使用一年 `immutable` 缓存，首页 HTML 不强缓存。
- SQLite `PRAGMA quick_check` 返回 `ok`，`material_jobs` 表存在。Nginx 配置检查通过；`/api/audio-source/` 单独关闭访问日志，并只反向代理到本机服务。
- 公网冒烟通过：首页和主构建资源返回 200；未登录材料能力接口返回 401；错误的音频短链返回 404；飞书 OAuth 回调与连接入口返回 404。
- 生产主机部署依赖时因 2 GiB 内存紧张出现服务重启，已在用户授权后通过云控制台正常重启实例。随后以低内存参数完成生产依赖安装，单独安装并校验 Linux `ffmpeg-static`，没有在生产主机构建前端。

## 可复现命令

```bash
npm test
npm run build
npx tsc --noEmit
```

浏览器脚本为 `scripts/verify-recording-ui.mjs`，只启动本地静态服务并拦截模拟 API，不导入生产 server、不访问真实账号、不调用付费模型。需要可用的 Playwright 与浏览器：

```bash
# PLAYWRIGHT_MODULE 可指向已有 Playwright 的 index.mjs；未设置时按正常包解析。
# 有系统 Chrome 时可以设置 BROWSER_CHANNEL=chrome。
node scripts/verify-recording-ui.mjs
```

`npx tsc --noEmit` 尚未通过，错误位于已有 `services/pdf/` 文件。对 `988c104` 基线与本分支分别运行，完整错误输出比对一致；没有使用 ignore/any 或修改检查规则来隐藏错误。

## 验证边界

没有为验收在生产库创建临时用户，因此没有执行“生产账号登录 → 浏览器上传 → 转写 → 人工确认 → 真实 AI 分析”的整链路写入测试。该链路已由真实 Express、SQLite、文件系统和模拟 ASR 的 HTTP 集成测试覆盖；生产侧另行验证了真实火山 ASR、Linux 音频处理、静态页面、鉴权边界、SQLite 完整性与代理规则。后续首次真实用户上传仍需监控 PM2 错误日志、火山用量和磁盘占用。

ASR 配置、Nginx URL 日志处理、Linux ffmpeg 安装及发布步骤见 [接入与运维](audio-minutes-operations.md)。
