# 未来需迭代内容

## 维护规则

- 状态只使用：待评估、计划中、已完成、暂不处理。
- 每项记录优先级、触发条件、当前缓解、目标方案和验收标准。
- 完成后保留记录并补充完成日期与提交号，不直接删除历史。

## P1：角色升级为服务端 token 绑定

- 状态：待评估
- 当前风险：强化版 A 可防误操作，但客户端角色值和 API 参数可被同一账号主动修改。
- 当前缓解：登录角色锁定、路由重定向、报告模式校验、缺失角色失败关闭。
- 触发条件：用户量明显增长、引入企业客户、角色拥有不同权限、出现审计或合规要求。
- 目标方案：为 `tokens` 增加 `analysis_mode`，登录与注册写入角色，认证中间件返回角色，分析、列表、详情、附件、删除与反馈接口统一校验。
- 验收标准：修改浏览器存储、URL 或 API 参数均不能访问 token 绑定角色之外的数据；退出后重新登录才能换角色。

## P1：历史密码哈希迁移

- 状态：暂不处理
- 当前风险：2026-08-05 已让新密码使用随机盐 scrypt，但按“不改现有用户数据”要求，历史无盐 SHA-256 哈希仍保留。
- 当前缓解：旧哈希只读兼容登录且不会自动改写；数据库不提交仓库，生产数据目录按私有权限维护。
- 触发条件：用户主动改密能力完成，或获得明确的数据迁移授权。
- 目标方案：通过用户主动改密或显式迁移消除历史 SHA-256，不在普通登录中静默改写。
- 验收标准：新密码继续保持 scrypt；历史账号完成获批迁移；日志不输出密码或哈希。新密码加固提交：`2981b41`、`659949f`。

## P1：Token 过期与摘要存储

- 状态：已完成
- 完成记录：2026-08-05，新 Token 只存摘要、12 小时绝对过期，旧明文 Token 不再认证，当前会话退出立即失效；提交 `2981b41`、`659949f`、`da9a70d`。
- 后续增强：全设备退出、管理员撤销和活跃会话查看不在本轮范围，需求出现时另立任务。

## P1：登录限流

- 状态：已完成
- 完成记录：2026-08-05，登录和集成 Token 同时按 IP 与规范化账号执行窗口限流，统一凭据错误，窗口后恢复；提交 `ac1c211`。
- 当前边界：计数保存在单个 Node 进程内，重启会重置；多实例前迁移共享存储，安全告警另行建设。

## P1：首用户管理员初始化

- 状态：已完成
- 完成记录：2026-08-05，公开注册永远创建普通用户；一次性 bootstrap 只允许空 users 表，非空库失败关闭；提交 `659949f`。
- 运维边界：现有生产库不得运行 bootstrap，发布不改变已有管理员权限。

## P2：Token 离开 localStorage

- 状态：已完成
- 完成记录：2026-08-05，浏览器改用 HttpOnly、生产 Secure、SameSite=Strict Cookie，前端清除旧 Token 存储；写请求增加 Origin 白名单；提交 `da9a70d`、`b8fa4d9`。
- 当前边界：无 Origin 的受控集成继续使用 Bearer Token；角色仍未绑定 Token。

## P2：前端第三方运行时依赖与 CSP

- 状态：计划中
- 当前进展：2026-08-05 已把 Tailwind 和 Inter 纳入本地构建，移除 import map，并在 Express 增加最小 CSP 等安全头；提交 `b8fa4d9`。
- 未完成：尚未部署和验收生产 Nginx/浏览器 CSP，版本化静态资源长期缓存仍未配置。
- 验收标准：生产页面不依赖未批准的第三方脚本；CSP 下登录、上传、分析、报告和 PDF 导出正常；版本化资源返回可复用的长期缓存头。

## P3：React Router RSC 安全公告跟踪

- 状态：暂不处理
- 当前风险：`npm audit` 报告 `react-router` / `react-router-dom` 的 high 公告 `GHSA-qwww-vcr4-c8h2`，攻击路径位于 RSC Action；当前项目使用 `BrowserRouter`，未使用 RSC API。
- 当前缓解：不启用 React Server Components 或 RSC Action，并在依赖升级时复查公告适用性。
- 触发条件：计划引入 RSC、官方发布兼容的修复版本，或公告范围扩大到当前 BrowserRouter 架构。
- 目标方案：升级到已修复且兼容的 React Router 版本，并完整回归登录、角色路由、历史和报告详情。
- 验收标准：`npm audit` 不再报告该公告，且当前完整自动化测试和生产构建全部通过。
