# Eval Bar AI 兼容式安全加固设计

- 状态：方案已确认，待实施。
- 日期：2026-08-05。
- 方案：兼容式安全加固，不迁移或改写现有用户数据。

## 背景与目标

生产站点已经通过 `https://evalbar.cn` 和 `https://www.evalbar.cn` 提供 TLS，但 Node 服务的 `3000` 端口仍可从公网通过明文 HTTP 访问，形成 HTTPS 绕过入口。代码审计和隔离复现同时发现：密码使用无盐 SHA-256、Token 无过期且明文存储、浏览器把 Token 保存到 `localStorage`、首用户自动成为管理员、登录与 AI 接口没有限流、反馈缺少 Schema 校验、招聘模式存在间接 Prompt Injection 风险，以及安全响应头、CORS、上传资源限制和前端第三方脚本管理不足。

本次目标是按风险顺序关闭这些攻击路径，同时保持现有用户、管理员权限、报告、反馈、附件和 Prompt 内容不变。允许部署后让现有登录会话失效并要求用户重新登录，但不删除或更新数据库中的旧 Token 记录。

## 数据保护边界

本次实施必须遵守以下约束：

- 不修改数据库 Schema，不执行数据迁移。
- 不更新或删除现有 `users`、`tokens`、`reports`、`feedback`、`report_attachments`、`system_prompt` 或 `candidate_system_prompt` 记录。
- 不读取或输出真实密码哈希、Token、候选人材料、报告正文和附件正文。
- 不改变现有用户的 `is_admin` 状态。
- 不自动重写现有 SHA-256 密码哈希。
- 不物理删除旧 Token；部署后的认证只接受新摘要格式，使旧明文 Token 立即失效。
- 新注册、新登录、新反馈和新报告产生的新记录属于正常产品行为，不是对既有数据的迁移；自动化验证只使用隔离数据库和合成数据。
- 生产防火墙、Nginx、PM2、环境变量和部署操作必须在本地验证完成后单独取得授权。

## 方案选择

### 采用：兼容式安全加固

在现有表结构内区分新旧密码格式：新密码使用带算法标识的 scrypt 编码，旧 SHA-256 密码继续兼容验证。新 Token 只把摘要写入现有 `tokens.token` 字段；认证只查询摘要格式，不再接受旧明文 Token。旧密码不自动升级，旧 Token 不删除，但部署后立即失去认证能力。

这样可以在不迁移既有数据的前提下保护所有新凭据，并通过凭据格式切换强制重新登录。代价是数据库中仍会保留不可用的历史 Token 和旧密码哈希；它们的物理清理与密码升级需要未来单独授权。

### 不采用：完整凭据迁移

直接改表、批量替换密码哈希、清理 Token、增加持久化额度表可以获得更干净的最终状态，但会修改现有数据，不符合本次约束。

### 不采用：仅外围加固

只关闭公网端口并增加 Nginx 安全头无法解决永久 Token、弱密码、首用户管理员、公开注册滥用、反馈拒绝服务和 Prompt Injection，不能视为完整修复。

## 漏洞基线清单

以下清单是 2026-08-05 外部低影响探测、代码审计和隔离合成数据复现的统一基线。后续实施、测试和发布都使用固定编号追踪，不因拆分任务而重新编号。

证据类型定义：

- **生产确认**：通过未写入数据的外部请求观察到当前生产行为。
- **隔离复现**：在临时 SQLite 和合成账号中成功复现，不代表读取或攻击了真实用户。
- **代码确认**：攻击条件由当前代码直接成立，但未对生产执行高负载、真实 AI 或破坏性验证。

| 编号 | 严重度 | 漏洞 | 攻击前提与影响 | 当前证据 | 初始状态 | 2026-08-05 当前状态 |
|---|---|---|---|---|---|---|
| SEC-01 | 严重 P0 | 公网 `3000` 仍提供明文 HTTP | 攻击者诱导用户访问正规域名的 `:3000`，链路中间人可窃取或篡改密码、Token、面试记录和简历 | 生产确认：`http://evalbar.cn:3000/` 返回 `200`；`server.js` 未指定监听主机 | 待修复 | 待生产验证：代码默认回环监听，尚未关闭生产安全组/防火墙端口 |
| SEC-02 | 高危 P1 | 公开注册且注册、登录、AI 无速率、并发和额度限制 | 外部攻击者可批量注册、爆破密码、消耗 AI 费用和服务资源 | 代码确认；隔离环境连续错误登录无 `429` | 待修复 | 本地验证通过：窗口额度、每日额度和单用户并发已接入；单进程计数为残余边界 |
| SEC-03 | 高危 P1 | 弱密码、无盐 SHA-256、永久明文 Token 与浏览器 `localStorage` | 数据库泄漏可导致快速离线破解；脚本注入或旧 Token 泄漏可长期冒充用户 | 代码确认；隔离复现 1 位密码、SHA-256、旧 Token 不过期、Token 累积 | 待修复 | 本地验证通过，残余风险已记录：历史 SHA-256 哈希保留且未改写 |
| SEC-04 | 高危 P1，条件触发 | 第一个注册用户自动成为管理员 | 新部署、空库或数据库恢复异常时，抢先注册者获得所有报告和 Prompt 管理权限 | 代码确认；隔离空库复现；未读取生产用户数据 | 待修复 | 本地验证通过：注册永远是普通用户，初始化脚本对非空库失败关闭 |
| SEC-05 | 高危 P1 | 间接 Prompt Injection 与模型 Markdown 外部资源加载 | 恶意简历或逐字稿可干扰招聘结论；模型输出的远程图片可触发浏览器外连 | 代码确认；隔离 ReactMarkdown 渲染确认远程图片请求；未调用真实生产模型 | 待修复 | 本地验证通过，残余风险已记录：模型语义注入只能分层缓解 |
| SEC-06 | 中危 P2 | 反馈接口缺少 Schema 校验，可持久化破坏管理员页面 | 普通用户提交字符串 `specificIssues` 或异常评分后，管理员页面 `.map()` 崩溃 | 隔离复现写入成功并触发确定性前端错误 | 待修复 | 本地验证通过：新反馈严格校验，历史异常值只在读取时归一化 |
| SEC-07 | 中危 P2 | 缺少 HSTS、CSP、点击劫持等安全头，运行时依赖第三方脚本 | 第三方脚本污染或 XSS 的影响被放大，页面可被嵌套欺骗点击 | 生产确认缺少关键响应头；`index.html` 加载 Tailwind CDN 和外部 import map | 待修复 | 待生产验证：应用头和本地资源构建已通过，Nginx 与外部响应尚未验收 |
| SEC-08 | 中危 P2 | multipart 内存上传只限制单文件大小 | 已注册攻击者可用大量字段、parts 或并发请求消耗 Node 内存和 CPU | 代码确认；为避免生产影响未执行负载复现 | 待修复 | 待生产验证：应用层 JSON/multipart 上限隔离通过，Nginx 请求体上限尚未配置验收 |
| SEC-09 | 低危 P3 | CORS 为通配符 `*` | 不能直接读取 `localStorage` Token，但允许任意站点调用 API，放大公开注册和接口滥用 | 生产确认恶意 Origin 预检返回 `Access-Control-Allow-Origin: *` | 待修复 | 待生产验证：宽松 CORS 已删除且恶意写 Origin 本地返回 `403`，外部响应待验收 |
| SEC-10 | 低危 P3 | 用户枚举、未知 API 返回 SPA `200`、生产文档保留旧 HTTP 地址 | 攻击者可确认用户名或邮箱；监控误判；用户和运维可能继续访问不安全入口 | 代码与隔离确认；多份生产文档仍写 `http://14.103.45.4:3000/` | 待修复 | 本地验证通过：注册冲突模糊化、未知 API JSON `404`、公开地址统一 HTTPS |

### 不作为已确认漏洞的依赖观察

| 编号 | 观察项 | 判断 | 后续处理 |
|---|---|---|---|
| OBS-01 | `npm audit` 报告 React Router 的 `GHSA-qwww-vcr4-c8h2` | 2026-08-05 复核：公告针对 RSC Action/unstable RSC code paths；源码只使用 BrowserRouter，未使用 RSC/Action API，因此不是当前已确认攻击路径 | `npm view react-router-dom version` 为 7.18.2，官方公告修复版本 8.3.0 尚不可用；不强制升级，触发条件见验证台账 |

## 漏洞—修复—验证追踪矩阵

每个漏洞必须同时关联代码修复、自动化验证和最终验收。表内任一必需环节未完成时，该漏洞不得标记为“已关闭”。

| 编号 | 修复内容 | 实施阶段 | 自动化与隔离验证 | 生产验收 |
|---|---|---|---|---|
| SEC-01 | Node 默认绑定 `127.0.0.1`；外部地址统一 HTTPS；云安全组或防火墙关闭 `3000/tcp` | 阶段 1、6 | 监听主机单测；静态扫描禁止公网 HTTP 地址；本地从非回环地址不可访问 | 域名和 IP 的 `:3000` 均拒绝或超时；80 只跳转 443；443 正常 |
| SEC-02 | 注册、登录按 IP/账号限速；AI 按 IP/用户限速、并发和每日额度控制；超限前停止 AI 调用 | 阶段 3、6 | 合成请求达到阈值后返回 `429`；验证 AI 客户端调用次数不增加；窗口恢复测试 | 低频正常请求成功；受控合成突发触发 `429`；供应商预算告警单独确认 |
| SEC-03 | 新密码 scrypt；旧 SHA-256 只兼容登录；新 Token 摘要存储和绝对过期；Cookie 会话；清除前端 Token 存储 | 阶段 2、6 | 新旧密码登录、弱注册拒绝、Token 摘要、旧 Token 拒绝、过期拒绝、Cookie 属性、前端存储静态测试 | 旧会话返回 `401`；原账号可重新登录；Cookie 为 HttpOnly/Secure/SameSite；页面存储无 Token |
| SEC-04 | 注册永远创建普通用户；提供失败关闭的一次性管理员初始化脚本但不执行 | 阶段 2、6 | 空库首注册 `isAdmin=false`；已有用户或管理员时初始化脚本拒绝；现有管理员夹具保持不变 | 只读确认现有管理员数量和权限未被代码发布改变；不在生产运行初始化脚本 |
| SEC-05 | 两套 Prompt 标记输入为不可信数据；输出合约验证；Markdown 禁止图片、原始 HTML和危险协议 | 阶段 4、6 | 恶意简历/逐字稿夹具不能改变输出契约；图片不生成请求节点；危险链接不可点击 | 使用无真实候选人内容、无真实 AI 费用的页面夹具确认浏览器无第三方图片请求 |
| SEC-06 | 服务端严格校验反馈；读取历史异常值时归一化；管理页面使用 `Array.isArray()` | 阶段 4、6 | 异常评分、字符串/超长/未知问题标签返回 `400`；历史异常夹具仍可安全展示 | 普通合法反馈成功；管理员反馈页正常；不改写历史反馈记录 |
| SEC-07 | 自托管构建期 Tailwind/字体；Helmet 或等价响应头；关闭技术指纹；生产 Nginx 同步安全头 | 阶段 5、6 | 构建产物不含 Tailwind CDN/`esm.sh`；响应头集成测试；CSP 下页面测试 | HTTPS 响应包含 HSTS、CSP、frame 限制、nosniff、Referrer Policy；页面功能正常 |
| SEC-08 | 增加 multipart `fields`、`parts`、`fieldSize` 限制、请求正文上限和并发保护；Nginx 限制请求体 | 阶段 3、6 | 超字段、超 parts、超字段长度和超文件请求分别返回 `400/413`；内存上传边界测试 | 小型合法上传成功；超限合成请求在 Nginx 或应用层被拒绝；不做生产压力测试 |
| SEC-09 | 删除宽松 CORS；非安全方法执行 Origin 白名单校验；API 保持同源 Cookie | 阶段 5、6 | 恶意 Origin 预检/写请求被拒绝；规范生产域名和本地开发 Origin 通过 | `evil.example` 不再获得 CORS 许可；正常域名登录、反馈和分析可用 |
| SEC-10 | 注册错误模糊化；未知 `/api/*` 返回 JSON `404`；统一生产域名并修正文档 | 阶段 1、5、6 | 重复账号和不存在账号使用不可枚举响应；未知 API 测试；文档 URL 静态扫描 | 外部未知 API 返回 JSON `404`；运维文档和页面入口只引用 HTTPS 规范域名 |

### 状态流转与关闭定义

每个 `SEC-*` 项按以下状态流转：

1. **待修复**：漏洞基线已确认，尚未完成代码修复。
2. **代码完成**：对应实现已完成，但还不能宣称漏洞关闭。
3. **本地验证通过**：自动化测试、构建和隔离复现均通过。
4. **待生产验证**：需要生产配置、部署或外部探测才能完成闭环。
5. **已关闭**：追踪矩阵要求的生产验收全部通过，且没有改动既有用户数据。
6. **残余风险已记录**：受本次不改数据或单实例架构限制而无法消除的风险，必须写清触发条件和后续方案，不能伪装为已关闭。

本地代码实施结束时，SEC-01、SEC-07、SEC-08 和 SEC-09 至多进入“待生产验证”；公网端口、Nginx 响应头和请求体限制未经真实环境验证不得关闭。SEC-03 中的历史 SHA-256 密码属于明确残余风险：新注册密码和新会话可以关闭对应攻击路径，但旧密码哈希只有在用户主动改密或未来获得迁移授权后才能彻底消除。

### 闭环交付物

实施阶段必须同步维护 `docs/superpowers/verification/2026-08-05-security-hardening.md`，按 `SEC-01` 至 `SEC-10` 逐项记录：关联提交、测试名称和命令、隔离复现结果、生产验收结果、最终状态及残余风险。实施计划的每个任务标题必须标注对应 `SEC-*` 编号；没有编号的安全改动不得混入本次范围。

最终交接文档必须分别列出“已关闭”“待生产验证”“残余风险已记录”三类，不得只用全量测试通过代替安全结论。以后复查时以本设计的漏洞编号为主键，将新的证据追加到验证文档；如果发现新攻击路径，新增编号而不是覆盖原始结论。

## 总体实施顺序

| 顺序 | 对应漏洞 | 主要工作 | 阶段完成门槛 | 数据影响 |
|---|---|---|---|---|
| 1 | SEC-01、SEC-10 文档部分 | 关闭明文应用入口；修正所有生产地址和外部冒烟命令 | Node 监听测试和 URL 静态扫描通过 | 无 |
| 2 | SEC-03、SEC-04 | Cookie、Token、密码和管理员初始化加固 | 认证测试通过；现有 SHA-256 用户夹具仍能登录；旧 Token 拒绝 | 不改既有记录 |
| 3 | SEC-02、SEC-08 | 注册、登录、AI、反馈限流及上传资源保护 | 超限请求稳定拒绝且不会调用 AI | 无新增业务表 |
| 4 | SEC-06、SEC-05 | 反馈 Schema、Prompt 不可信边界和 Markdown 渲染限制 | 持久化 UI 拒绝服务与外链请求隔离复现被阻断 | 不清理旧反馈 |
| 5 | SEC-07、SEC-09、SEC-10 接口部分、OBS-01 | 本地前端依赖、安全响应头、同源策略、API 错误和依赖复核 | CSP 构建与 HTTP 集成测试通过；依赖结论有证据 | 无 |
| 6 | SEC-01 至 SEC-10 | 全量自动化、构建、隔离对抗复测和生产操作清单 | 每个编号都有验证记录和明确状态 | 仅合成数据 |

## 阶段 1：关闭明文应用入口

Node 服务新增明确的监听主机配置：默认 `HOST=127.0.0.1`，`app.listen(PORT, HOST)`。同机 Nginx 继续通过 `127.0.0.1:3000` 访问后端；只有 Docker 等明确需要容器端口映射的环境才显式传入 `HOST=0.0.0.0`。

仓库中的生产 URL 单一来源统一为 `https://evalbar.cn`。服务器本机健康检查继续使用 `http://127.0.0.1:3000`，外部冒烟只使用 HTTPS 域名。部署文档必须明确：公网 `3000` 应拒绝连接，不能把应用监听地址当作公开入口。

生产侧最终仍需关闭云安全组或主机防火墙的 `3000/tcp`。代码绑定和网络访问控制是两层独立防线，任一层都不能省略。

## 阶段 2：认证与会话加固

### Cookie 会话

登录和注册成功后，服务端设置名为 `evalbar_session` 的 Cookie：

- `HttpOnly`，禁止前端 JavaScript 读取。
- 生产环境启用 `Secure`。
- `SameSite=Strict`。
- `Path=/`。
- `Max-Age` 与服务端绝对有效期一致。

认证中间件优先读取 Cookie。为兼容非浏览器 API，可继续接受 `Authorization: Bearer`，但只接受部署后签发、能匹配摘要记录且未到期的新 Token；浏览器前端不再保存或发送 Bearer Token。所有前端请求使用同源 Cookie，退出登录由服务端清除 Cookie。

前端启动时删除旧的 `auth_token` 和 `admin` 本地键，不复用其中的 Token；通过 `/api/auth/me` 恢复用户身份。登录角色 `evalbar_auth_mode` 继续作为产品路由状态保留，但不作为权限凭据。部署后现有用户需要重新登录。

### Token 存储与有效期

新登录生成随机原始 Token，只把 `sha256:<hex>` 摘要存入现有 `tokens.token` 字段，原始 Token 只进入 HttpOnly Cookie。验证时只查询摘要格式，不回退查询旧明文 Token。所有新 Token 根据 `created_at` 执行绝对有效期校验，时间无效、缺失或超过有效期时统一返回 `401`。

本次不删除旧 Token，也不更新其字段。旧 Token 即使仍存在于数据库，也会因为格式不再受信任而立即失去认证能力。登出只删除当前新会话对应的摘要记录；这是用户主动触发的正常会话行为。

### 密码存储与规则

新注册密码至少 10 个字符，服务端和前端使用同一规则。新密码使用 Node 内置 `crypto.scrypt`、随机盐和常量时间比较，编码格式包含版本、参数、盐和摘要。现有 64 位 SHA-256 哈希继续验证，使原账号仍可登录；登录成功后不自动重写旧哈希。

未来如允许修改用户数据，再采用显式迁移或用户主动修改密码的方式消除遗留 SHA-256 哈希。本次不得把“新密码安全”描述成“所有历史密码已迁移”。

### 管理员初始化

公开注册永远创建普通用户，不再根据用户数量自动设置管理员。现有管理员状态保持不变。

新增独立的一次性管理员初始化脚本，仅允许在本机显式运行，并要求目标用户名、密码通过命令参数或交互输入提供。脚本发现已有用户或管理员时必须失败关闭，不能隐式提升普通用户。本次只实现和测试脚本，不在生产执行。

## 阶段 3：限流、AI 配额与上传资源保护

限流按用途分开，避免一个接口耗尽全站额度：

- 注册：按 IP 的低频窗口限制。
- 登录：同时按 IP 和规范化用户名限制，成功登录后只清理对应用户名计数。
- 反馈：按用户限制短时间写入次数。
- AI 分析：按 IP 和用户双重限制，并限制单用户并发。

当前生产是单 PM2 进程，因此第一版使用进程内限流和并发计数，不修改数据库。服务重启会重置计数，文档必须明确这一边界；未来多实例部署前需要更换为 Redis 或其他共享存储。

AI 分析还应限制逐字稿、岗位、能力模型、JD 和解析后简历文本的字符数，并为单用户设置每日进程内调用额度。超过限制统一返回 `429` 或 `413`，不得调用 AI 提供商。供应商控制台预算上限属于生产配置，发布阶段单独处理。

Multer 增加 `fields`、`parts`、`fieldSize` 等限制，Nginx 最终设置对应 `client_max_body_size`。上传失败只返回稳定错误码，不回显底层解析异常。

## 阶段 4：输入、模型与渲染安全

### 反馈 Schema

服务端要求：

- `reportId` 为非空字符串，并继续执行报告所有权校验。
- `rating` 为 1 到 5 的整数。
- `comments` 为可选字符串，具有固定长度上限。
- `specificIssues` 为可选字符串数组，限制项目数、单项长度并使用允许值集合。

读取历史反馈时执行兼容归一化：非数组 `specificIssues` 作为空数组返回，非法评分只用于管理展示兜底，不修改数据库原值。管理页面继续使用 `Array.isArray()` 防止历史异常数据触发 `.map()` 崩溃。

### Prompt Injection

Candidate 与 Recruiter Prompt 都必须声明简历、职位描述和面试记录为不可信数据，只能作为证据，不能改变系统规则、输出格式或权限。现有 JSON 包装继续保留，并在模型输出后验证必需章节和允许格式。

本次不尝试通过关键字黑名单“清洗”自然语言，因为攻击指令可以变形。防线由系统指令隔离、结构化数据边界、输出验证、渲染限制和对抗样本共同组成。

### Markdown 渲染

报告 Markdown 禁止渲染远程图片和原始 HTML。链接只允许 `http:`、`https:` 和 `mailto:`，使用安全的 `target` 与 `rel`；不符合协议的链接按纯文本处理。模型输出不能触发浏览器向任意图片地址发送请求。

## 阶段 5：浏览器和 HTTP 加固

应用 API 全部同源访问，不再使用宽松 `cors()`。不支持第三方站点携带 Cookie 调用 API。对所有非 GET/HEAD 请求校验 `Origin`；生产只接受规范域名，开发环境接受本机 Vite 地址。

Express 增加安全响应头：CSP、HSTS、`frame-ancestors`、`X-Content-Type-Options`、Referrer Policy 和合理的 Permissions Policy，并关闭 `X-Powered-By`。生产 Nginx 在发布阶段配置同等或更严格的头部，避免只依赖应用层。

移除运行时 Tailwind CDN 和无用 import map。Tailwind 改为构建期生成，Inter 字体改为本地依赖或可靠的系统字体回退，确保生产页面不执行第三方 CDN JavaScript。CSP 以自托管脚本和样式为默认，不为兼容旧 CDN 放宽到任意来源。

未匹配的 `/api/*` 返回 JSON `404`，不得落入 React SPA 的 `index.html`。认证和注册错误使用稳定、模糊的客户端消息；详细原因只允许出现在不含敏感数据的服务端诊断中。

## 错误处理与兼容行为

- 旧明文 Token 或新 Token 超期：返回统一 `401`，前端清理认证状态并进入登录页。
- Cookie 缺失：不从旧 `localStorage` Token 静默恢复。
- 旧 SHA-256 密码正确：允许登录并签发新 Cookie，不重写密码记录。
- 密码不符合新规则：只阻止新注册，不阻止现有旧密码账号登录。
- 没有管理员的空库：普通注册不会获得管理员；需要运维显式执行初始化脚本。
- 限流触发：返回 `429` 和稳定错误码，不暴露账号是否存在。
- 历史异常反馈：管理页安全显示，不自动修复数据库。
- Prompt 输出不合约：拒绝保存或返回明确的分析失败，不把未经验证的结构当作可信结果。

## 测试策略

所有行为变更遵循测试驱动开发：先写最小失败测试，确认失败原因正确，再实现最小代码并运行全量回归。

自动化测试至少覆盖：

- 默认监听 `127.0.0.1`，显式 `HOST` 可覆盖。
- 生产 URL 文档不再出现公网 HTTP `:3000`。
- Cookie 属性、登录恢复、退出清理和前端不保存 Token。
- 新 Token 摘要存储、旧明文 Token 拒绝、绝对过期和无效时间拒绝。
- scrypt 新密码、旧 SHA-256 登录兼容、新密码长度和常量时间校验。
- 空库首注册不是管理员，初始化脚本失败关闭。
- 注册、登录、反馈、AI 的限流和单用户并发。
- 超限请求不会调用 AI 客户端。
- multipart 字段、parts、字段长度和文件大小限制。
- 反馈 Schema 与历史异常反馈归一化。
- Prompt 不可信数据边界和输出合约。
- Markdown 图片禁用、危险链接拒绝、安全链接属性。
- CORS/Origin、安全响应头、`X-Powered-By` 移除和 API JSON 404。
- 构建产物不加载 Tailwind CDN 或 `esm.sh`。

完成后运行：

```bash
npm test
npm run build
npm audit
```

隔离对抗复测使用临时 SQLite 和合成账号，验证弱密码、永久 Token、首用户管理员、暴力登录、AI 滥用、反馈持久化拒绝服务、Prompt 注入和 Markdown 外链均被阻断。不得对生产执行注册、登录爆破、真实 AI、上传压力或跨用户数据测试。

## 生产发布与验收边界

本地实施不包含生产发布。代码和构建验证完成后，生产变更需要单独授权，范围包括：

- 在本地或 CI 构建 `dist/`，禁止在 1.9 GB、无 swap 的生产主机构建。
- 上传并原子替换构建产物与代码，重启 PM2。
- 检查 Node 只监听 `127.0.0.1:3000`。
- 关闭云安全组和主机防火墙的公网 `3000/tcp`。
- 更新 Nginx 安全响应头、请求体限制和规范域名跳转。
- 设置 AI 提供商预算告警或费用上限。

生产验收必须同时满足：

- `https://evalbar.cn` 首页、登录和受保护 API 正常。
- `http://evalbar.cn` 跳转 HTTPS。
- 外网访问 `evalbar.cn:3000` 和服务器 IP `:3000` 拒绝连接或超时。
- 登录响应只通过安全 Cookie 建立会话，页面存储中不存在 Token。
- 部署前会话无法继续认证，用户可使用原账号密码重新登录。
- 未授权端点、跨用户报告和管理员端点继续正确拒绝。
- 生产数据的表数量、用户数量、报告数量、附件数量和关键文件哈希在部署前后保持一致；验证只比较计数和哈希，不展开敏感内容。

## 明确不包含

- 不迁移或清理现有密码、Token、反馈和报告。
- 不新增数据库字段、索引或额度表。
- 不把登录角色升级为服务端权限属性。
- 不引入 Redis、多实例共享限流或独立认证服务。
- 不执行生产数据修复、生产压力测试或真实 AI 对抗调用。
- 不在本次本地实施中修改 `.env*`、密钥、云安全组、Nginx 或 PM2。
